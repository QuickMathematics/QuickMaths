# Lesson illustration assets

Generated Matplotlib SVGs for the content-matched lesson library. Rebuild from the repository root with `python -m scripts.build_lesson_illustrations`.

Filenames include a content digest. The generated `docs/lesson-illustration-data.js` records full SHA-256 digests, sizes, teaching captions and alternative descriptions. Keep referenced files available for installed and exported lessons.
